spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/webflux
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              ReactiveMongoTemplate: TRACE
